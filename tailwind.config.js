module.exports = {
    content: [
        "./index.html", // Matches your index.html in the root
        "./**/*.{html,js}", // Matches any other HTML/JS files in subdirectories
    ],
    theme: {
        extend: {},
    },
    plugins: [],
};
